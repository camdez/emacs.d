version: 2.1

jobs:
  test:
    docker:
      - image: silex/emacs:master-ci
    steps:
      - checkout

      - run:
          name: Run tests
          command: make test

      - run:
          name: Test config initialization
          command: emacs --batch --load init.el

workflows:
  test:
    jobs:
      - test
